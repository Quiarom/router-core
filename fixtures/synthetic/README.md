# Synthetic fixtures

These files are hand-authored parser inputs, not captures from physical
hardware. They intentionally exercise known response shapes and edge cases.
Real sanitized captures belong in `fixtures/captured/`.

# Captured fixtures

Only sanitized real captures from the target router belong here. Replace every
credential, token, or secret with opaque placeholders such as
`<SESSION_TOKEN>` and `<ROUTER_ADMIN_PASSWORD>`. Tests use captured files when
they exist and skip cleanly when this directory is empty.
